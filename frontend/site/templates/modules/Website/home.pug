extends ../../_layout

prepend html
  - custom.title = 'Home'
  - custom.body_cls = 'body--home'

block main
  section.home
    h2 Home
    | !{config.getSVG("trash")}

block scripts
  +requireTag('site.home')
