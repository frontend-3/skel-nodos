Each validator script in this directory should add an array to the $validators array. This is to build a list of validators for an element's InputFilter.

The name of each file should correspond to an entry on the 'validators' array defined on the YAML file.

